name: ADVECTOR
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - python-dotenv
  - tqdm
  - ipython
  - pip
  - zarr
  - aiohttp
  - requests
  - pyopencl
  - pip:
      - motuclient
      - ecco_v4_py
prefix: /opt/miniconda3/envs/ADVECTOR
