name: ADVECTOR
channels:
  - conda-forge
  - defaults
dependencies:
  - ipython
  - python=3
  - pandas
  - matplotlib
  - pip
  - python-dotenv
  - tqdm
  - cartopy
  - dask
  - xarray
  - numpy
  - pip:
      - motuclient
prefix: /opt/miniconda3/envs/ADVECTOR
