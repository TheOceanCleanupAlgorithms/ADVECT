name: ADVECTOR
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - python-dotenv
  - tqdm
  - ipython
  - pip
  - zarr
  - aiohttp
  - requests
  - pip:
      - motuclient
prefix: /opt/miniconda3/envs/ADVECTOR
