name: ADVECTOR
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - wget
  - numpy
  - pandas
  - xarray
  - netcdf4
  - dask
  - pyopencl
  - pocl
  - matplotlib
  - cartopy
  - tqdm
  - pytest
prefix: /opt/miniconda3/envs/ADVECTOR
