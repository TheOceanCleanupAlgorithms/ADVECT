name: ADVECTOR
channels:
  - conda-forge
  - defaults
dependencies:
  - ipython
  - python=3
  - xarray
  - scipy
  - pandas
  - matplotlib
  - pip
  - python-dotenv
  - pip:
      - motuclient
prefix: /opt/miniconda3/envs/ADVECTOR
